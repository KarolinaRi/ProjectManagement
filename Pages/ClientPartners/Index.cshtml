@page
@model ProjectManagement.Pages.ClientPartners2.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.ClientPartner[0].CpName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ClientPartner[0].CpAddress)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ClientPartner[0].CpDetails)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.ClientPartner) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.CpName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.CpAddress)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.CpDetails)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.Id">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
